# 0.3.2 validation — 2026-09-15

The reported 0.3.1 failure was an additional build-backend download (`flit_core`) while installing the Click source distribution. All declared source resources had already downloaded successfully. The log establishes a PyPI download timeout, but does not establish which network or device-management policy caused it. The subsequent Homebrew JSON exception occurred while reporting that build error.

Validation completed:

- Repository verification passed: 235 tests, 99.83% application coverage, and no Bandit findings.
- Four release-generator tests cover the complete locked runtime dependency set, rejection of missing universal wheels, and index-free wheel installation in the generated formula.
- Homebrew installed a temporary keg-only validation formula successfully. It used the release files locally and the actual locked dependency-wheel URLs and hashes. No normal user binaries were relinked by this test.
- All nine Python install steps processed local wheel files, with `PIP_NO_INDEX=1` and build isolation disabled. No backend downloads occurred.
- `brew test` passed. The installed CLI successfully ran `init` and `set FOO bar` in a temporary directory. Installed versions matched the lock file.
- The pure Python wheel contains no native helper. The helper was rebuilt with the macOS 27 SDK, retaining minimum macOS 26 and arm64 architecture, and its Developer ID signature verified.
- Helper notarization accepted: `032447df-cc44-4418-ad51-5340dbdde62a`.

Release checksums:

- `envrcctl-0.3.2.tar.gz`: `b539dbfb2cf6874b2f97e2bfa12c90835d209bcb957dab22038c95bf94ff4eb8`
- `envrcctl-0.3.2-py3-none-any.whl`: `c3e389259385025e504eb668edceeed663acadc7fc8dc563f3d504263d6310c3`
- `envrcctl-macos-auth-0.3.2-arm64.tar.gz`: `e2b94b933f851ca297a4cdb6e2c079aaf14e0680efb82c08a5cabef38181dfb6`

The source archive still provides completion files; the final generator follows the current Homebrew component ordering. Runtime wheels and the signed helper are unchanged by that formatting adjustment.
